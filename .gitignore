# Rust build artifacts
/target

# Coverage
/coverage
*.profraw
lcov.info

# Local audit database written by `codehelion scan`, wherever it is run from
.codehelion/

# AI assistants / agent tooling (repo stays AI-free in git)
/AGENTS.md
/CLAUDE.md
/.claude/
/.codex/
/.cursor/
/.aider*
/.github/copilot-instructions.md

# Local-only working area (design docs, plans, scratch)
/backup

# Evaluation corpus: external OSS snapshots are fetched locally, never committed
/corpus/external/
# Labelled cases commit their verdicts; the sources are cut from a pinned
# commit by corpus/scripts/materialize-labeled.sh and stay out of the tree.
/corpus/labeled/*/snapshot/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
