[package]
name = "codehelion-fixtures"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Locates the analysis fixtures, and holds what has to stay true of them."
# Test scaffolding. The fixtures are inputs to this repository's own tests and
# mean nothing to anyone installing the tool.
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3.27.0"

[lints]
workspace = true
