[package]
name = "codehelion-artifact-macho"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Mach-O artifact parser backend for codehelion."

[dependencies]
codehelion-artifact = { version = "0.1.0", path = "../codehelion-artifact" }
object = { version = "0.39", default-features = false, features = ["read", "macho"] }

[dev-dependencies]
object = { version = "0.39", default-features = false, features = ["write", "macho"] }
proptest = "1.6"

[lints]
workspace = true
